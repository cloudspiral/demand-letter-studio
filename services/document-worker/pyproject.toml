[project]
name = "steno-document-worker"
version = "0.1.0"
description = "Page-aware evidence extraction and structure-preserving DOCX operations for Demand Letter Studio."
requires-python = ">=3.12,<3.14"
dependencies = [
  "lxml==6.0.1",
  "pillow==11.3.0",
  "pypdf==6.0.0",
  "python-docx==1.2.0",
]

[tool.uv]
package = false
